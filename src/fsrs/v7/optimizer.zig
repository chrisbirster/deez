//! FSRS-7 parameter optimization.
//!
//! The optimizer is intentionally version-scoped. Training is implemented in a
//! later milestone; this module exists now so optimizer APIs do not leak into
//! the scheduler core.
